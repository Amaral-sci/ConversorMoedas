//
//  ContentView.swift
//  ConversorMoedas
//
//  Created by Jonathan Amaral on 01/12/25.
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
                .padding()
            Text("Hello, world!")

            Button("PRECIONE PARA INICIAR") {
                
            }
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
